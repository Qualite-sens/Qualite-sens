$(document).ready(function() {
 // hides the slickbox as soon as the DOM is ready
 // shows the slickbox on clicking the noted link
$(' button ').click(function() {
$('.unputdevice').toggle('slow');
return false;
  });
    });
